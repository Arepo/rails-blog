In topic: <%= @post.topic %>
<%= @post.title %>
<%= @post.body %>
<%= link_to_if(current_user, 'Edit post', edit_post_path(@post)) {} %>
<%= link_to_if current_user, 'Delete post',
    post_path(@post),
    method: :delete,
    data: { confirm: 'Definitely delete?' } {}
%>
