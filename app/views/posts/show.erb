In topic: <%= @post.topic %>
<%= @post.title %>
<%= @post.body %>
<%= link_to_if(true, 'Edit post', edit_post_path(@post)) {} %>
<%= link_to_if true, 'Delete post',
    post_path(@post),
    method: :delete,
    data: { confirm: 'Definitely delete?' } {}
%>
